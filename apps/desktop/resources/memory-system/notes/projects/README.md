# Project Notes

Use one file per project when context must persist across multiple sessions but
is too project-specific for global `MEMORY.md`.

Name files with a stable lowercase slug, for example:

```text
morninghq.md
client-analysis.md
personal-automation.md
```

Keep each project note concise. For extensive architecture or operating rules,
prefer documentation and `AGENTS.md` inside the repository itself.
